1. Create a RemoteEvent into ReplicatedStorage and name it "JoinRemoteEvent".
2. Create a RemoteEvent into ReplicatedStorage and name it "LeaveRemoteEvent".
3. Put the "PlayerJoinedScript.lua" and the "PlayerLeftScript.lua" into ServerScriptService.
